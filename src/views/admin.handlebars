<div id="resultado">
  <form id="deleteProduct">
    <input id="inputDelete" name="pid" placeholder="Id producto a Eliminar" />
    <button type="submit">Eliminar Producto</button>
  </form>
  <form
    action="/api/products/products"
    method="post"
    enctype="multipart/form-data"
  >
    <input
      type="text"
      id="titleCreate"
      name="title"
      required="required"
      placeholder="title"
    />
    <input
      type="text"
      id="descriptionCreate"
      name="description"
      required="required"
      placeholder="description"
    />
    <input
      type="number"
      id="priceCreate"
      name="price"
      required="required"
      placeholder="Price"
    />
    <input
      type="number"
      id="codeCreate"
      name="code"
      required="required"
      placeholder="code"
    />
    <input
      type="number"
      id="stockCreate"
      name="stock"
      required="required"
      placeholder="stock"
    />
    <input
      type="text"
      id="categoryCreate"
      name="category"
      required="required"
      placeholder="category"
    />
    <input type="file" name="thumbnail" />
    <button type="submit">Crear producto</button>
  </form>
  <form
    action="/api/products/update/products"
    method="post"
    enctype="multipart/form-data"
  >
    <input type="text" id="id" name="id" required="required" placeholder="id" />
    <input
      type="text"
      id="title"
      name="title"
      required="required"
      placeholder="title"
    />
    <input
      type="text"
      id="description"
      name="description"
      required="required"
      placeholder="description"
    />
    <input
      type="number"
      id="price"
      name="price"
      required="required"
      placeholder="Price"
    />
    <input
      type="number"
      id="code"
      name="code"
      required="required"
      placeholder="code"
    />
    <input
      type="number"
      id="stock"
      name="stock"
      required="required"
      placeholder="stock"
    />
    <input
      type="text"
      id="category"
      name="category"
      required="required"
      placeholder="category"
    />
    <input type="file" name="thumbnail" />
    <button type="submit">Editar producto</button>
  </form>
  <form action="/api/users/premium" method="post">
    <input
      type="email"
      name="uid"
      required="required"
      placeholder="email del usuario"
    />
    <input
      type="text"
      name="rol"
      required="required"
      placeholder="Rol nuevo para el usuario"
    />
    <button type="submit">Cambiar rol</button>
  </form>
   
  <button id="mostrarUsers" type="button">Mostrar usuarios</button>
  
  <button id="deleteUser" type="button">Eliminar users inactivos</button>

</div>
<script>

  document.getElementById("deleteProduct").addEventListener("submit",(event)=> {
  event.preventDefault(); 
  const deletePid =document.getElementById("inputDelete").value;

  deleteProduct(deletePid); });
  const deleteProduct=async (productId)=>{
   const resp=await fetch(`/api/products/${productId}`,{ method : "DELETE" })
   if(resp.ok){const resDelete=await resp.json()
    const messageDelete=resp.message

    const h1Delete=document.createElement("h1")
    h1.textContent=messageDelete

    document.getElementById("resultado").innerHTML=""
    document.getElementById("resultado").appendChild(h1Delete)
   } } 
  


  document.getElementById("deleteUser").addEventListener("click",(event)=> {
  event.preventDefault(); 
  deleteUsers(); });

  const deleteUsers = async () => { 
    try {

  const response = await fetch(`/api/users/delete`, {
  method: "DELETE", }); 
  if (response.ok) { const res = await response.json();
      const message = res.message

      const h1 = document.createElement('h1')
      h1.textContent = message

      document.getElementById('resultado').innerHTML = ''
      document.getElementById('resultado').appendChild(h1)
    }
   else { console.log("Error al eliminar usuarios"); } 
   } catch (error) {
  console.error("Error:", error);console.log("Ocurrió un error en la solicitud"); }
  };


   document.getElementById("mostrarUsers").addEventListener("click",(event)=> {
  event.preventDefault(); 
  mostrarUser(); });

  const mostrarUser = async () => { 
    

 window.location.href="https://entrega-opcional.onrender.com/api/users/userinfo"
  };

</script>